<form method="post" action="{{ path('ligue_delete', {'id': ligue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligue.id) }}">
    <button class="btn">Delete</button>
</form>

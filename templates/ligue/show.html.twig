{% extends 'base.html.twig' %}

{% block title %}Ligue{% endblock %}

{% block body %}
    <h1>Ligue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligue.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ligue.nom }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ ligue.dateCreation ? ligue.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligue_index') }}">back to list</a>

    <a href="{{ path('ligue_edit', {'id': ligue.id}) }}">edit</a>

    {{ include('ligue/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}LigueMembre{% endblock %}

{% block body %}
    <h1>LigueMembre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligue_membre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligue_membre_index') }}">back to list</a>

    <a href="{{ path('ligue_membre_edit', {'id': ligue_membre.id}) }}">edit</a>

    {{ include('ligue_membre/_delete_form.html.twig') }}
{% endblock %}

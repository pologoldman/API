<form method="post" action="{{ path('ligue_membre_delete', {'id': ligue_membre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligue_membre.id) }}">
    <button class="btn">Delete</button>
</form>
